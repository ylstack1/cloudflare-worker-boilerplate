/**
 * EDGE-MANIFEST SDK
 *
 * JavaScript/TypeScript SDK for EDGE-MANIFEST.
 * Currently stubbed pending SDK feature implementation.
 */

export type SDKConfig = Record<string, unknown>;

export function createSDK(config: SDKConfig) {
  return {
    config,
  };
}
