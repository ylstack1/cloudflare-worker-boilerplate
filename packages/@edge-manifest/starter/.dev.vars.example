# Local Development Environment Variables Template
# Copy this file to .dev.vars and configure your local environment
# cp .dev.vars.example .dev.vars

# JWT Secret for authentication (use a strong secret in production)
JWT_SECRET=your-jwt-secret-here

# Optional: Inject a manifest directly (JSON string)
# EDGE_MANIFEST={"id":"app","name":"App","version":"1.0.0","entities":[]}

# Optional: Alternative manifest variable name
# MANIFEST={"id":"app","name":"App","version":"1.0.0","entities":[]}
