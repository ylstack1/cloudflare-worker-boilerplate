# Starter QUICKSTART

From the repo root:

```bash
pnpm install
pnpm dev
```

Open:

- http://127.0.0.1:8787/admin
- http://127.0.0.1:8787/docs

Run the curl smoke tests:

```bash
./test-endpoints.sh
```
